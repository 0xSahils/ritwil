# =============================================================================
# Ritwil Backend – Environment variables
# Copy this file to .env and fill in the values. Do not commit .env to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
# PostgreSQL connection string. Example:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#   For Supabase: use the "Connection string" from Project Settings > Database
DATABASE_URL=

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
# Port the API server listens on
PORT=4000

# Set to "production" when deploying (enables secure cookies, CORS, etc.)
NODE_ENV=development

# Allowed frontend origin(s) for CORS. Comma-separated for multiple.
# Examples: https://app.ritwil.com  or  https://app.ritwil.com,https://www.ritwil.com
CLIENT_ORIGIN=http://localhost:5173

# -----------------------------------------------------------------------------
# JWT (auth tokens)
# -----------------------------------------------------------------------------
# Use long random strings in production (e.g. openssl rand -base64 32)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Token lifetimes (optional; defaults shown)
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d
# Used for refresh cookie maxAge (days, numeric)
JWT_REFRESH_TTL_DAYS=30

# -----------------------------------------------------------------------------
# Forgot password
# -----------------------------------------------------------------------------
# Base URL of your frontend (used in reset links). No trailing slash.
# Example: http://localhost:5173 or https://app.ritwil.com
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Email (optional – for sending password-reset emails via Resend)
# -----------------------------------------------------------------------------
# If not set, forgot-password will not send email (in dev the API may return
# the reset link in the response for testing).
# Get your API key from https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# From email must be verified in Resend dashboard (e.g. noreply@yourdomain.com)
# RESEND_FROM_EMAIL=noreply@yourdomain.com
# Or with display name: Ritwil <noreply@yourdomain.com>
