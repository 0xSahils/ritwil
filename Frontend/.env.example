# =============================================================================
# Ritwil Frontend – Environment variables
# Copy to .env and set values. Vite only loads variables that start with VITE_
# =============================================================================

# -----------------------------------------------------------------------------
# API base URL (optional)
# -----------------------------------------------------------------------------
# Leave unset when:
#   - Development with Vite proxy (default: /api is proxied to backend)
#   - Production when backend serves this app (same origin, /api works)
#
# Set when frontend is on a different domain than the API, e.g.:
#   VITE_API_BASE_URL=https://api.ritwil.com/api
#   VITE_API_BASE_URL=http://localhost:4000/api
# VITE_API_BASE_URL=
